import { SearchJobsPipe } from './search-jobs.pipe';

describe('SearchJobsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchJobsPipe();
    expect(pipe).toBeTruthy();
  });
});
